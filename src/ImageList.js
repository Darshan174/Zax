// import React from "react";

// function ImageList({ imageUrls }) {
//   return (
//     <div className="image-list">
//       {imageUrls.map((url, index) => (
//         <img key={index} src={url} alt={`Image ${index}`} className="uploaded-image" />
//       ))}
//     </div>
//   );
// }

// export default ImageList;
